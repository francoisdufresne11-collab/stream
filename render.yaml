services:
  - type: web
    name: streamcaster
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        value: "admin2026"
    healthCheckPath: /
    autoDeploy: true
    plan: starter
